<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="coba.mpchart">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RadarChartActivity"></activity>
        <activity android:name=".populateGraphActivity" />
        <activity android:name=".PiechartActivity" />
        <activity android:name=".StackedBarActivity" />
        <activity
            android:name=".GroupBarchartActivity"
            android:theme="@style/NoTheme" />
        <activity
            android:name=".BarchartActivity"
            android:theme="@style/NoTheme" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/NoTheme" />
        <activity
            android:name=".HomeActivity"
            android:theme="@style/NoTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>